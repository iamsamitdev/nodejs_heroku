<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" integrity="sha512-iBBXm8fW90+nuLcSKlbmrPcLa0OT92xO1BIsZ+ywDWZCvqsWgccV3gFoRBv0z+8dLJgyAHIhR35VZc2oM/gI1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container mt-5">
        <!-- Page Heading -->
        <div class="d-sm-flex align-items-center justify-content-between mb-4">
            <h1 class="h3 mb-0 text-gray-800"><%- heading %></h1>
            <a href="/backend/mssql_create_product" class="d-none d-sm-inline-block btn btn-success shadow-sm"><i class="fas fa-plus fa-sm text-white-50"></i> Create product</a>
        </div>
    
        <!-- Content Row -->
        <div class="row">
            
            <% 
                if(data.length)
                {
            %>
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr class="bg-primary text-white">
                            <th>Image</th>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Category</th>
                            <th>Price</th>
                            <th>Qty</th>
                            <th>Created</th>
                            <th>Updated</th>
                            <th>Manage</th>
                        </tr>
                    </thead>
                    <tbody>
                        <%
                        for(let row=0; row < data.length; row++)
                        {
                        %>
                        <tr>
                            <td><img src="<%=data[row].ProductPicture%>" width="50"></td>
                            <td><%=data[row].ProductID%></td>
                            <td><%=data[row].ProductName%></td>
                            <td><%=category[data[row].CategoryID]%></td>
                            <td><%=formatPrice(data[row].UnitPrice)%></td>
                            <td><%=data[row].UnitInStock%></td>
                            <td><%=moment(data[row].CreatedDate).format('DD/MM/YYYY')%></td>
                            <td><%=moment(data[row].ModifiedDate).format('DD/MM/YYYY')%></td>
                            <td style="width: 200px">
                                <a href="#" class="btn btn-warning">Edit</a>&nbsp;
                                <a href="#" class="btn btn-danger">Delete</a>
                            </td>
                        </tr>
                        <%
                        }
                        %>
                    </tbody>
                </table>
            <%
                }else{
            %>
            <p class="text-center">No products found</p>
            <%
                }
            %>
    
        </div>
    
    </div>
</body>
</html>